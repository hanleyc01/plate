# plate

Vector symbolic interpreter for a subset of lisp using holographic reduced
representations

# Interacting

# Hacking

This project uses [`uv`](https://docs.astral.sh/uv/). Download it,
and run:
```bash
$ uv sync
```